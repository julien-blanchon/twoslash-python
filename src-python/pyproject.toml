[project]
name = "src-python"
version = "0.1.0"
description = "Generate hover information for Python code"
requires-python = ">=3.12"
dependencies = [
    "cyclopts>=3.10.1",
    "multilspy>=0.0.14",
]
